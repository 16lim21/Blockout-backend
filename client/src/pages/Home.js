/**
 * Home page that users see once they have logged in
 * @exports Home
 */
import GoogleButton from "../components/GoogleButton";

const Home = () => {
    return (
        <div>
            <h1>Home Page</h1>
            <GoogleButton />
        </div>
    );
};

export default Home;
